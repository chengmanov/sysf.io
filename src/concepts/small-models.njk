---
layout: layouts/base.njk
title: Small Models, Measured — Why a Fine-Tuned 8B Beats an API You Rent
description: Why task-tuned small language models outperform frontier APIs on narrow regulated workflows — the specialisation effect, the fine-tuning vs. RAG division of labour, the economics, and when a small model is the wrong choice.
faq:
  - q: Is an 8B model really good enough for German legal or clinical text?
    a: For a scoped task, usually yes — but we never ask you to take that on faith. Every engagement starts by building an evaluation set from your own documents and measuring base model, RAG, fine-tune and the combination against it. If the small model doesn't clear the bar we agree up front, you see that in the report before you spend on production. The Canton of Ticino's fine-tuned Apertus-8B runs cantonal translation in production at 94% accuracy — the pattern is real, but the evaluation decides for your task.
  - q: What about reasoning-heavy work?
    a: Deep multi-hop reasoning over open-ended questions is where model size still pays. Our answer is architectural, not ideological — a small model handles the routine volume, and requests it can't handle confidently escalate to a larger open-weight model running on the same infrastructure. Nothing escalates to a cloud API. The <a class="link-underline" href="/concepts/sovereign-architecture/">sovereign runtime page</a> describes how that router works.
  - q: Doesn't fine-tuning become obsolete every time a better base model ships?
    a: The expensive assets survive model swaps. Your evaluation set, your curated training data and your retrieval corpus all carry over unchanged; re-running a LoRA fine-tune on a newer base model is days of work, not a rebuild. That's also why we stay model-agnostic — Apertus, Llama or Mistral, whichever wins on your eval set at the time.
---
{% from "partials/components.njk" import sectionHead, faqList, cta %}
{% from "partials/charts.njk" import deltaBars %}

<section class="border-b border-ink-900/10 dark:border-white/10">
  <div class="container-x py-16 md:py-24">
    <p class="eyebrow">Concepts · Small models</p>
    <h1 class="mt-4 max-w-4xl text-4xl font-bold leading-[1.08] tracking-tightest text-ink-900 md:text-5xl dark:text-white">
      Small models, measured.
    </h1>
    <p class="mt-6 max-w-2xl text-lg prose-muted md:text-xl">
      A fine-tuned 4–8B model that knows your documents beats a frontier model that has never seen them — on the narrow, high-volume tasks that actually matter in regulated work. Here's why, with the honest limits included.
    </p>
  </div>
</section>

<!-- Specialization -->
<section class="container-x py-20 md:py-28">
  <div class="grid gap-12 md:grid-cols-12">
    <div class="md:col-span-5">
      {{ sectionHead("The effect", "Specialisation beats scale on narrow tasks.", "General benchmarks reward breadth. Your workflow rewards depth — your templates, your terminology, your formats, your edge cases.") }}
    </div>
    <div class="md:col-span-6 md:col-start-7 space-y-5 text-base prose-muted">
      <p>Out of the box, a general-purpose small model is a generalist that happens to speak your language. Fine-tuned on a few thousand examples of <em>your task</em>, it becomes a specialist that has read your corpus. How much that buys depends on where the base model starts — which is exactly what the ablation in every Pilot measures, on your documents, before you commit.</p>
      <p>The public reference point in Switzerland: the <a class="link-underline" href="https://www.cscs.ch/science/computer-science-hpc/2026/apertus-powers-in-house-ai-translation-for-ticino" target="_blank" rel="noopener">Canton of Ticino's translation service</a> went from 90% to <strong class="font-semibold text-ink-900 dark:text-white">94% accuracy after fine-tuning Apertus-8B</strong> on cantonal data, running entirely on cantonal infrastructure. Four points at the top of the scale — in a production service, the difference between spot-checking output and re-doing it.</p>
      <p>A frontier API has two structural handicaps on this ground: it has never seen your documents, and it never can — because they're the documents you can't send away. The small model's advantage isn't intelligence. It's access.</p>
    </div>
  </div>
  <div class="mt-12">
    {% set ticino = [
      {label:"Base Apertus-8B", v:90, cls:"bg-teal-300"},
      {label:"Fine-tuned on cantonal data", v:94, cls:"bg-red-600", tag:"+4 pts"}
    ] %}
    {{ deltaBars(ticino, "Canton of Ticino — translation accuracy on cantonal text, before and after fine-tuning Apertus-8B. <a class='link-underline' href='https://www.cscs.ch/science/computer-science-hpc/2026/apertus-powers-in-house-ai-translation-for-ticino' target='_blank' rel='noopener'>Source: CSCS</a>.") }}
  </div>
</section>

<!-- Division of labour -->
<section class="border-y border-ink-900/10 bg-ink-900/[0.02] py-20 dark:border-white/10 dark:bg-white/[0.02] md:py-28">
  <div class="container-x">
    {{ sectionHead("The method", "Fine-tuning teaches behaviour. Retrieval supplies facts.", "The two are routinely conflated — and the conflation is where AI projects go wrong.") }}
    <div class="mt-12 grid gap-5 md:grid-cols-3">
      {% set roles = [
        {t:"Fine-tuning", d:"Teaches the task: your terminology, document formats, tone, output structure, refusal behaviour. It does not reliably memorise facts — using it as a knowledge store is a design error we refuse to ship."},
        {t:"Retrieval (RAG)", d:"Supplies the facts: the current version of your policies, contracts and records, cited back to the source page. Knowledge updates the moment the document does — no retraining."},
        {t:"The ablation", d:"Every Pilot scores all four arms — base, retrieval, fine-tune, combined — on your evaluation set. The table says which system is worth buying."}
      ] %}
      {% for r in roles %}
      <div class="rounded-lg border border-neutral-200 bg-white p-8 shadow-sm dark:border-white/10 dark:bg-ink-950">
        <h3 class="text-lg font-semibold text-ink-900 dark:text-white">{{ r.t }}</h3>
        <p class="mt-2 text-sm prose-muted">{{ r.d }}</p>
      </div>
      {% endfor %}
    </div>
    <p class="mt-8 max-w-2xl text-base prose-muted">Measured, the split is stark: in <a class="link-underline" href="/reports/finma-ablation/">our FINMA ablation</a>, fine-tuning alone took format compliance from ~2% to 100% yet left answer-plus-citation accuracy at 1.6% — behaviour learned, facts not. Retrieval alone: 22.4%. Combined: 45.4%.</p>
  </div>
</section>

<!-- Economics -->
<section class="container-x py-20 md:py-28">
  <div class="grid gap-12 md:grid-cols-12">
    <div class="md:col-span-5">
      {{ sectionHead("The economics", "An order of magnitude cheaper to run — and faster.", "Small models don't just fit your compliance envelope. They fit your budget — in francs and in milliseconds.") }}
    </div>
    <div class="md:col-span-6 md:col-start-7 space-y-5 text-base prose-muted">
      <p><strong class="font-semibold text-ink-900 dark:text-white">Serving cost:</strong> running a 7–8B model costs roughly an order of magnitude less than a 70B-class model — the difference between one workstation-class GPU and a multi-GPU node, between a purchase order and a procurement round.</p>
      <p><strong class="font-semibold text-ink-900 dark:text-white">Latency:</strong> every cloud API call pays a round-trip tax — network hops, TLS, queueing behind other tenants — before your tokens even reach a GPU; first tokens routinely arrive only after several hundred milliseconds. A small model in your own rack skips that tax entirely, and its faster prefill and decode compound on top — which is what makes high-volume document pipelines and interactive assistants feel instant.</p>
      <p><strong class="font-semibold text-ink-900 dark:text-white">The sovereignty multiplier:</strong> every parameter you don't need is compliance you don't have to buy. A task-tuned small model runs on one machine — the workstation shape in <a class="link-underline" href="/concepts/sovereign-architecture/">the sovereign runtime</a> — which opens the air-gapped and edge deployments (shop floor, clinic, field service) where hosted platforms and US APIs structurally cannot follow.</p>
    </div>
  </div>
</section>

<!-- Honest limits -->
<section class="border-y border-ink-900/10 bg-ink-900/[0.02] py-20 dark:border-white/10 dark:bg-white/[0.02] md:py-28">
  <div class="container-x">
    {{ sectionHead("The limits", "When a small model is the wrong choice.", "An expert who won't tell you when their favourite tool loses isn't an expert. Small models lose here:") }}
    <div class="mt-12 grid gap-5 md:grid-cols-2 lg:grid-cols-4">
      {% set limits = [
        {t:"Open-ended assistance", d:"\"Answer anything about anything\" rewards scale. If you need a general chat assistant over public knowledge, a frontier model is better at it."},
        {t:"Broad world knowledge", d:"Small models know less. Retrieval closes the gap only where your corpus has the answer — it can't retrieve what nobody wrote down."},
        {t:"Deep multi-hop reasoning", d:"Long chains of novel reasoning still favour larger models. Routine volume goes small; the hard tail escalates to a larger local model."},
        {t:"Non-sensitive, low-volume work", d:"If the data can leave and the volume is trivial, an API subscription is simpler. We'll say so in the scoping call."}
      ] %}
      {% for l in limits %}
      <div class="rounded-lg border border-neutral-200 bg-white p-6 shadow-sm dark:border-white/10 dark:bg-ink-950">
        <h3 class="text-base font-semibold text-ink-900 dark:text-white">{{ l.t }}</h3>
        <p class="mt-2 text-sm prose-muted">{{ l.d }}</p>
      </div>
      {% endfor %}
    </div>
    <p class="mt-8 max-w-2xl text-base prose-muted">The dividing line isn't opinion — it's the evaluation set. That's why every engagement starts with one: <a class="link-underline" href="/concepts/evaluation/">evaluation-first delivery</a>.</p>
  </div>
</section>

<!-- FAQ -->
<section class="container-x py-20 md:py-28">
  <div class="grid gap-12 md:grid-cols-12">
    <div class="md:col-span-4">{{ sectionHead("FAQ", "Small-model questions.") }}</div>
    <div class="md:col-span-7 md:col-start-6">{{ faqList(faq) }}</div>
  </div>
</section>

{{ cta("Would a small model clear your bar?", "One workflow, your documents, our harness. We'll build the evaluation set and show you the ablation — in your jurisdiction.") }}
