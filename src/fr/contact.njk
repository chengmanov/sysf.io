---
layout: layouts/base.njk
order: 5
title: Contact
description: Réservez un appel de cadrage avec sysf.io pour définir votre premier cas d'usage d'IA souveraine — modèles sur mesure affinés sur Apertus, déployés dans votre juridiction suisse ou européenne, ou on-prem.
ogType: website
---
{% from "partials/components.njk" import icon %}
<section class="container-x py-16 md:py-24">
  <div class="grid gap-14 md:grid-cols-12">
    <!-- Left -->
    <div class="md:col-span-5">
      <p class="eyebrow">Contact</p>
      <h1 class="mt-4 text-4xl font-bold leading-[1.08] tracking-tightest text-ink-900 md:text-5xl dark:text-white">
        Cadrons votre premier cas d'usage souverain.
      </h1>
      <p class="mt-6 text-lg prose-muted">
        Décrivez-nous le workflow et les données que vous ne pouvez pas confier. Nous reviendrons avec une proposition à périmètre fixe — sans engagement.
      </p>

      <div class="mt-10 space-y-6">
        <div>
          <div class="text-xs font-semibold uppercase tracking-[0.18em] text-ink-500">E-mail</div>
          <a class="mt-1 block text-lg font-semibold text-ink-900 link-underline dark:text-white" href="mailto:{{ site.email }}?subject=Demande%20IA%20souveraine">{{ site.email }}</a>
        </div>
        <div>
          <div class="text-xs font-semibold uppercase tracking-[0.18em] text-ink-500">À quoi vous attendre</div>
          <ul class="mt-2 space-y-2 text-sm prose-muted">
            {% for x in ["Un appel de 30 minutes avec l'ingénieur qui construira votre système", "Une proposition de pilote à périmètre fixe sur vos données — sous une semaine", "Un non clair avant la 20e minute si un modèle ouvert affiné n'est pas le bon choix"] %}
            <li class="flex items-start gap-2.5"><span class="mt-0.5 shrink-0 text-teal-700 dark:text-teal-300">{{ icon("check", "h-4 w-4") }}</span>{{ x }}</li>
            {% endfor %}
          </ul>
        </div>
        <p class="text-sm text-ink-500">Basés en Suisse. Au service des organisations suisses et européennes.</p>
      </div>
    </div>

    <!-- Right: form -->
    <div class="md:col-span-6 md:col-start-7">
      <div class="border border-ink-900/10 p-6 md:p-8 dark:border-white/10">
        <!--
          TODO: this static site has no backend yet. The form below composes an email via
          the visitor's mail client (works everywhere, no data leaves the page). To switch to
          hosted submissions instead: create a free endpoint at https://formspree.io, set
          action="https://formspree.io/f/YOUR-ID" method="POST" on the form, and delete the
          enquiryForm <script> block at the bottom of this file.
        -->
        <form id="enquiryForm" action="#" class="space-y-5">
          <label class="block">
            <span class="text-sm font-medium text-ink-900 dark:text-white">Nom</span>
            <input type="text" name="name" required autocomplete="name"
                   class="mt-1.5 w-full rounded-md border border-neutral-300 bg-white px-3 py-2.5 text-sm text-ink-900 outline-none transition focus:border-red-500 focus:ring-2 focus:ring-red-500/25 dark:border-white/15 dark:bg-ink-900 dark:text-white" />
          </label>
          <label class="block">
            <span class="text-sm font-medium text-ink-900 dark:text-white">Organisation</span>
            <input type="text" name="organization" autocomplete="organization"
                   class="mt-1.5 w-full rounded-md border border-neutral-300 bg-white px-3 py-2.5 text-sm text-ink-900 outline-none transition focus:border-red-500 focus:ring-2 focus:ring-red-500/25 dark:border-white/15 dark:bg-ink-900 dark:text-white" />
          </label>
          <label class="block">
            <span class="text-sm font-medium text-ink-900 dark:text-white">Secteur</span>
            <select name="sector"
                    class="mt-1.5 w-full rounded-md border border-neutral-300 bg-white px-3 py-2.5 text-sm text-ink-900 outline-none transition focus:border-red-500 focus:ring-2 focus:ring-red-500/25 dark:border-white/15 dark:bg-ink-900 dark:text-white dark:[&>option]:bg-ink-900">
              {% for s in ["Banque & Finance", "Santé & Pharma", "Juridique", "État & Secteur public", "Assurance", "Industrie", "Autre"] %}
              <option>{{ s }}</option>
              {% endfor %}
            </select>
          </label>
          <label class="block">
            <span class="text-sm font-medium text-ink-900 dark:text-white">Que souhaitez-vous construire ?</span>
            <textarea name="message" rows="4" required
                      class="mt-1.5 w-full rounded-md border border-neutral-300 bg-white px-3 py-2.5 text-sm text-ink-900 outline-none transition focus:border-red-500 focus:ring-2 focus:ring-red-500/25 dark:border-white/15 dark:bg-ink-900 dark:text-white"></textarea>
          </label>
          <button type="submit" class="btn-primary w-full">Envoyer la demande</button>
          <p class="text-xs text-ink-500">L'envoi ouvre votre client de messagerie — votre demande va directement à {{ site.email }}, nulle part ailleurs. Nous ne partageons pas vos coordonnées.</p>
        </form>
      </div>
    </div>
  </div>
</section>

<script>
  // Compose the enquiry as an email — no backend, no third-party form handler.
  (function () {
    var form = document.getElementById("enquiryForm");
    if (!form) return;
    form.addEventListener("submit", function (e) {
      e.preventDefault();
      var v = function (n) { var el = form.elements[n]; return el && el.value ? el.value.trim() : ""; };
      var subject = "Demande IA souveraine — " + (v("organization") || v("name"));
      var body = "Nom : " + v("name") + "\nOrganisation : " + v("organization") + "\nSecteur : " + v("sector") + "\n\nCe que nous aimerions construire :\n" + v("message");
      window.location.href = "mailto:{{ site.email }}?subject=" + encodeURIComponent(subject) + "&body=" + encodeURIComponent(body);
    });
  })();
</script>
